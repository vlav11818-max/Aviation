You are a story architect. Your task is to create a detailed structural outline for a story, dividing it into sections that follow a specific narrative structure.

STORY CONCEPT:
{concept_json}

NARRATIVE STRUCTURE: {structure_name}
STRUCTURE TEMPLATE:
{structure_template}

TARGET LENGTH: {target_words} total words
NUMBER OF SECTIONS: {num_sections}
LANGUAGE: {language}

CULTURAL CONTEXT:
{cultural_instructions}

INSTRUCTIONS:
1. Divide the story into exactly {num_sections} sections following the "{structure_name}" narrative structure.
2. Each section must have a clear purpose within the overall structure (as defined by the structure template above).
3. Distribute the target word count of {target_words} across sections proportionally — climax and resolution sections may be longer.
4. Every section must advance the plot AND develop at least one character.
5. Transitions between sections must be smooth and logical — specify how each section connects to the previous and next.
6. Key events must be concrete and specific, not vague summaries.
7. Characters present in each section must be listed — ensure the protagonist appears in most sections.
8. Act labels must correspond to the phases defined in the structure template.

Respond with ONLY a valid JSON object (no markdown fences, no commentary) matching this exact structure:

{{
    "structure_type": "{structure_name}",
    "total_target_words": {target_words},
    "sections": [
        {{
            "index": 0,
            "title": "Short descriptive title for this section.",
            "act_label": "Act or phase label from the structure template (e.g., Act I — Setup).",
            "target_words": 800,
            "key_events": [
                "Specific event 1 that happens in this section.",
                "Specific event 2 that happens in this section."
            ],
            "characters_present": ["Character Name 1", "Character Name 2"],
            "transition_from": "How this section opens or connects from the previous section. Leave empty for section 0.",
            "transition_to": "How this section ends and leads into the next section."
        }}
    ]
}}

Requirements:
- Section indices must start at 0 and be consecutive.
- The sum of all section target_words must equal approximately {target_words} (within 5% tolerance).
- Each section must have at least 2 key_events.
- Each section must list at least 1 character in characters_present.
- The first section's transition_from must be empty string.
- The last section's transition_to must be empty string.
- All text content must be written in {language}.
